# API Documentation

Endpoints aren't implemented yet — the API gateway currently only exposes
`GET /health` plus stub routers under `/api` that return `501`. Real
endpoints, and auto-generated OpenAPI docs alongside them, land in Phase 3.
